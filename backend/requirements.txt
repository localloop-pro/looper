# Core dependencies (runtime — installed in production)
# H-014 R1 follow-up: CVE bumps — cleared 9 known vulns in runtime deps.
#   - fastapi 0.115.0 → 0.133.1: pulls starlette ≥1.0.1 (PYSEC-2026-161 + CVE-2024-47874 + CVE-2025-54121 cleared)
#   - starlette explicit pin ≥1.3.1: clears CVE-2026-48818, CVE-2026-48817, CVE-2026-54283, CVE-2026-54282
#   - python-dotenv 1.0.1 → 1.2.2: clears CVE-2026-28684 (symlink race in set_key/unset_key)
#   - Looper code verified: main + routes/{search,reviews,users,map}.py load with 16 routes, FastAPI 0.133.1
fastapi==0.133.1
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
pydantic==2.9.0
python-dotenv==1.2.2
starlette>=1.3.1

# LLM / AI (runtime — API client only)
openai==1.51.0
httpx==0.27.0

# Database (runtime)
aiosqlite==0.20.0

# Geo (runtime)
geopy==2.4.1

# Training / ML workloads moved to requirements-ml.txt
# Install only when actually fine-tuning or running inference with local models:
#   pip install -r requirements-ml.txt