# Core dependencies (runtime — installed in production)
fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
pydantic==2.9.0
python-dotenv==1.0.1

# LLM / AI (runtime — API client only)
openai==1.51.0
httpx==0.27.0

# Database (runtime)
aiosqlite==0.20.0

# Geo (runtime)
geopy==2.4.1

# Training / ML workloads moved to requirements-ml.txt
# Install only when actually fine-tuning or running inference with local models:
#   pip install -r requirements-ml.txt