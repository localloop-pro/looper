# Brain (TypeDB) dependencies — install only when TYPEDB_ENABLED=true
# Verified against TypeDB Core 2.29.1 on 2026-08-12. Keep the driver exact so
# migration/query behaviour cannot drift between local, CI, and Coolify.
typedb-driver==2.28.4

# Tools (news audio worker) — install for Coolify tools service
# openai is already in backend/requirements.txt
supabase>=2.0.0
